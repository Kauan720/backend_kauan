// Declaração das variáveis
let base = 10;
let altura = 5;

// Cálculos
let area = base * altura;
let perimetro = 2 * (base + altura);

// Saída usando Template Strings
console.log(`Área do retângulo: ${area}`);
console.log(`Perímetro do retângulo: ${perimetro}`);