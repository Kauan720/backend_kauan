// Temperatura em Celsius
let celsius = 25;

// Conversão para Fahrenheit
let fahrenheit = celsius * 1.8 + 32;
// Exibindo com Template String
console.log(`A temperatura de ${celsius}°C equivale a ${fahrenheit}°F.`);